<%= simple_form_for @book do |f| %>
<%= select_tag(:category_id,options_for_select(@categories),:prompt=>"Select and category") %>
    <%= f.input :title ,label: "Title"%>
    <%= f.input :description %>
    <%= f.input :author %>
    <%= f.button :submit %>
<% end %>